# Django Environment Variables for Production
# Copy this file to .env and update the values for your production environment

# SECURITY WARNING: Generate a new secret key for production!
# Generate using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# Or visit: https://djecrety.ir/
SECRET_KEY=your-super-secret-key-here-minimum-50-characters-long

# Set to False for production, True for development
DEBUG=False

# Comma-separated list of allowed hosts for production (REQUIRED when DEBUG=False)
# Include your domain, www.domain, and any subdomains
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,api.yourdomain.com

# Comma-separated list of allowed CORS origins for production
# Must use HTTPS in production
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com

# Database settings (uncomment if using PostgreSQL)
# DATABASE_URL=postgres://username:password@localhost:5432/voting_app

# Redis settings for production caching and rate limiting
REDIS_URL=redis://localhost:6379/1

# Email settings (for notifications, password reset)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-specific-password
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=Voting App <noreply@yourdomain.com>

# Security settings (automatically applied when DEBUG=False)
# These are already configured in settings.py for production
